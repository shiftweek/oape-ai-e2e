apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: "shift-ai-v2"

resources:
  - secret-configs.yaml
  - deployment.yaml

configMapGenerator:
  - name: shift-server-config
    files:
      - ./config/team-repos.csv
    options:
      disableNameSuffixHash: true
